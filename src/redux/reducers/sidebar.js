const initialState = {
	items: ['Парковка', 'Страховка', 'Бензин', 'Обслуживание']
}

const sidebar = (state = initialState, action) => {
	switch(action.type) {
		default: return state;
	}
}

export default sidebar;